package com.oyealex.pipe;

/**
 * PipeTestBase
 *
 * @author oyealex
 * @since 2023-04-28
 */
public abstract class PipeTestBase {
    protected static final String[] ELEMENTS = "these are unit tests for pipe filter api, and these unit tests can test if the pipe api work rightly".split(
            "[ ,]");

    protected static final String[] OTHER_ELEMENTS = "this is some strings who are ready to prepend or append into pipes".split(
            "[ ,]");
}
