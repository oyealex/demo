use std::io;
use rand::Rng;
use std::io::{Write, BufReader, BufRead, ErrorKind};
use std::fs::File;
use std::cmp::Ordering;

fn main() {
    let au8: u8 = 5;
    let au16: u16 = 10000;
    println!("{}", au16 as u8);
    let vec1 = vec![1, 2, 3, 45, 5];
}