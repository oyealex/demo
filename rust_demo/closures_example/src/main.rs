use rand::Rng;
use std::cmp::Ordering;
use std::fs::File;
use std::io;
use std::io::{BufRead, BufReader, ErrorKind, Write};

fn main() {
    let au8: u8 = 5;
    let au16: u16 = 10000;
    println!("{}", au16 as u8);
    let vec1 = vec![1, 2, 3, 45, 5];
}
